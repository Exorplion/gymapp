# herramientas de agentes (locales, no forman parte de la app)
.claude/
.agents/
.superpowers/
.worktrees/

# instalación temporal de Playwright para verificación manual en la raíz
# (la app vainilla no tiene package.json propio; web/ sí y no debe ignorarse)
/node_modules/
/package.json
/package-lock.json
tmp-verify/
